<style>
  html .category-sub-title {
  	color: {{settings.brand_color_slategrey}};


  
  .anchore-bg {
    width: 500px;
    height: 500px;
    margin: auto;
    border-radius: 50%;
    opacity: 0.6;
    background: {{settings.brand_color}};
    filter: blur(100px);
    left: unset !important;
    transform: unset !important;
	}

  .grid_bg {
    background: {{settings.brand_color}};
    color: {{settings.brand_color_softwhite}};
    border: solid 1px {{settings.brand_color_slategrey}};
  }
</style>


  </div>
  <div class="absolute anchore-bg" style="top: 160px;"></div>
</section>


<section class="pb-20 mb-10">
  <div class="container mx-auto text-center px-6 py-4 md:py-0">
  <div class="py-8 mb-5">
        {{breadcrumbs}}
      </div>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {{#each sections}}
        <a href="{{url}}" class="grid_bg">
          <h3 class="category-card-title mb-5">{{name}}</h3>
          {{#if description}}
          	<p class="category-card-description text-left">{{description}}</p>
          {{else}}
          {{/if}}
        </a>
      {{/each}}
    </div>
  </div>
</section>
